<UserControl x:Class="FeedbackFactory.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#D7E4F2">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBox x:Name="UsernameInput" Width="200" Margin="5" />
            <PasswordBox x:Name="PasswordInput" Width="200" Margin="5"  />
            <ComboBox x:Name="RoleInput" Width="150" Margin="5">
                <ComboBoxItem Content="Lehrer" Tag="0" />
                <ComboBoxItem Content="Admin" Tag="1" />
            </ComboBox>
            <Button Content="Benutzer hinzufügen" Click="AddUserButton_Click" Width="150" Margin="5" />
        </StackPanel>

        
        <DataGrid Grid.Row="1" ItemsSource="{Binding BenutzerListe}" 
                  AutoGenerateColumns="True" 
                  HorizontalAlignment="Stretch" 
                  VerticalAlignment="Stretch" 
                  Margin="10" />
    </Grid>
</UserControl>