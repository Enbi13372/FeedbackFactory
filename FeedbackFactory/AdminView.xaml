<UserControl x:Class="FeedbackFactory.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#D7E4F2">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

       
        <StackPanel Orientation="Horizontal" Margin="10">
            <StackPanel Margin="5">
                <TextBlock Text="Benutzername:" />
                <TextBox x:Name="UsernameInput" Width="200" />
            </StackPanel>
            <StackPanel Margin="5">
                <TextBlock Text="Passwort:" />
                <PasswordBox x:Name="PasswordInput" Width="200" />
            </StackPanel>
            <StackPanel Margin="5">
                <TextBlock Text="Rolle:" />
                <ComboBox x:Name="RoleInput" Width="150">
                    <ComboBoxItem Content="Lehrer" Tag="0" />
                    <ComboBoxItem Content="Admin" Tag="1" />
                </ComboBox>
            </StackPanel>
            <Button Content="Benutzer hinzufügen" Click="AddUserButton_Click" Width="150" Margin="5" VerticalAlignment="Bottom" />
        </StackPanel>

        
        <DataGrid Grid.Row="1" ItemsSource="{Binding BenutzerListe}" 
                  AutoGenerateColumns="True" 
                  HorizontalAlignment="Stretch" 
                  VerticalAlignment="Stretch" 
                  Margin="10" />
    </Grid>
</UserControl>